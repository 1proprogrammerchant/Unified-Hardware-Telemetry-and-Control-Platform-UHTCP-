# UHTCP server default configuration (server component)
http_addr: ":8080"
core_base_url: "http://127.0.0.1:9000"
metrics:
  enabled: true
  path: "/metrics"
ws:
  enabled: true
  path: "/ws"
ipc:
  workers: 4
  queue_size: 1024
logging:
  level: "info"
